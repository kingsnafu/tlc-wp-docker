import { existsSync, mkdirSync, writeFileSync, cpSync } from 'node:fs';
import { resolve, join } from 'node:path';
import { fileURLToPath } from 'node:url';
import { dirname } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));
const TEMPLATE_DIR = join(__dirname, 'templates', 'default');

/**
 * Scaffold the Eleventy project from the default template.
 * Copies template files and generates site.css from theme config.
 */
export function scaffold(config, projectDir) {
  // Copy template files if eleventy.config.js doesn't exist yet
  if (!existsSync(join(projectDir, 'eleventy.config.js'))) {
    console.log('Scaffolding Eleventy template...');
    cpSync(TEMPLATE_DIR, projectDir, { recursive: true, force: false });
  }

  // Always regenerate site.css from theme config
  generateCss(config, projectDir);

  // Generate calendar config if enabled
  if (config.calendar) {
    generateCalendarConfig(config, projectDir);
  }
}

function generateCss(config, projectDir) {
  const theme = config.theme || {};
  const colors = theme.colors || {};
  const fonts = theme.fonts || {};

  const css = `/* Generated by wp-to-static from site-config.yaml */
:root {
  --color-primary: ${colors.primary || '#1a1a2e'};
  --color-secondary: ${colors.secondary || '#e94560'};
  --color-accent: ${colors.accent || '#0f3460'};
  --font-heading: '${fonts.heading || 'Playfair Display'}', serif;
  --font-body: '${fonts.body || 'Open Sans'}', sans-serif;
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: var(--font-body);
  line-height: 1.6;
  color: #333;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 1rem;
}

h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-heading);
  color: var(--color-primary);
  margin: 1.5em 0 0.5em;
  line-height: 1.2;
}

a { color: var(--color-accent); }
a:hover { color: var(--color-secondary); }

img { max-width: 100%; height: auto; }

/* Header */
.site-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1rem 0;
  border-bottom: 2px solid var(--color-primary);
  margin-bottom: 2rem;
}
.site-header h1 { margin: 0; font-size: 1.5rem; }
.site-header h1 a { text-decoration: none; color: var(--color-primary); }

/* Navigation */
nav ul { list-style: none; display: flex; gap: 1.5rem; }
nav ul ul { display: none; position: absolute; background: #fff; box-shadow: 0 2px 8px rgba(0,0,0,0.1); padding: 0.5rem; }
nav li:hover > ul { display: block; }
nav a { text-decoration: none; font-weight: 500; }

/* Main content */
main { min-height: 60vh; padding: 1rem 0; }

/* Blog listing */
.post-list { list-style: none; }
.post-list li { margin-bottom: 2rem; padding-bottom: 1rem; border-bottom: 1px solid #eee; }
.post-meta { color: #666; font-size: 0.9rem; margin-bottom: 0.5rem; }

/* Footer */
.site-footer {
  margin-top: 3rem;
  padding: 2rem 0;
  border-top: 2px solid var(--color-primary);
  text-align: center;
  color: #666;
  font-size: 0.9rem;
}

/* Forms */
.gform-field { margin-bottom: 1rem; }
.gform-field label { display: block; margin-bottom: 0.25rem; font-weight: 500; }
.gform-field input, .gform-field select, .gform-field textarea {
  width: 100%; padding: 0.5rem; border: 1px solid #ccc; border-radius: 4px; font-size: 1rem;
}
.gform-field textarea { min-height: 120px; }
.gform-submit { background: var(--color-secondary); color: #fff; border: none; padding: 0.75rem 2rem; border-radius: 4px; font-size: 1rem; cursor: pointer; }
.gform-submit:hover { opacity: 0.9; }

/* Responsive */
@media (max-width: 768px) {
  .site-header { flex-direction: column; gap: 1rem; }
  nav ul { flex-wrap: wrap; justify-content: center; }
}
`;

  const cssDir = join(projectDir, 'src', 'css');
  mkdirSync(cssDir, { recursive: true });
  writeFileSync(join(cssDir, 'site.css'), css, 'utf8');
}

function generateCalendarConfig(config, projectDir) {
  const calConfig = {
    widget_url: config.calendar.widget_url,
    api_url: config.calendar.api_url,
  };
  const dataDir = join(projectDir, 'src', '_data');
  mkdirSync(dataDir, { recursive: true });
  writeFileSync(join(dataDir, 'calendar.json'), JSON.stringify(calConfig, null, 2), 'utf8');
}
