{# Renders a Gravity Form definition as HTML form #}
{% macro renderForm(form, action) %}
<form class="gform" action="{{ action or '#' }}" method="POST">
  <h2>{{ form.title }}</h2>
  {% for field in form.fields %}
    {% if field.type == 'section' %}
      <hr>
      {% if field.label %}<h3>{{ field.label }}</h3>{% endif %}
    {% elif field.type == 'html' %}
      {# skip HTML fields #}
    {% elif field.type == 'page' %}
      {# skip page breaks #}
    {% else %}
      <div class="gform-field">
        <label for="field_{{ form.id }}_{{ field.id }}">
          {{ field.label }}{% if field.required %} <span class="required">*</span>{% endif %}
        </label>

        {% if field.type == 'textarea' %}
          <textarea id="field_{{ form.id }}_{{ field.id }}" name="input_{{ field.id }}"
            {% if field.placeholder %}placeholder="{{ field.placeholder }}"{% endif %}
            {% if field.required %}required{% endif %}></textarea>

        {% elif field.type == 'select' %}
          <select id="field_{{ form.id }}_{{ field.id }}" name="input_{{ field.id }}"
            {% if field.required %}required{% endif %}>
            <option value="">{{ field.placeholder or 'Select...' }}</option>
            {% for choice in field.choices %}
              <option value="{{ choice.value }}">{{ choice.text }}</option>
            {% endfor %}
          </select>

        {% elif field.type == 'radio' %}
          {% for choice in field.choices %}
            <label class="radio-label">
              <input type="radio" name="input_{{ field.id }}" value="{{ choice.value }}"
                {% if field.required and loop.first %}required{% endif %}>
              {{ choice.text }}
            </label>
          {% endfor %}

        {% elif field.type == 'checkbox' %}
          {% for choice in field.choices %}
            <label class="checkbox-label">
              <input type="checkbox" name="input_{{ field.id }}[]" value="{{ choice.value }}">
              {{ choice.text }}
            </label>
          {% endfor %}

        {% elif field.type == 'name' or field.type == 'address' %}
          {% for input in field.inputs %}
            <div class="sub-field">
              <label for="field_{{ form.id }}_{{ input.id }}">{{ input.label }}</label>
              <input type="text" id="field_{{ form.id }}_{{ input.id }}" name="input_{{ input.id }}">
            </div>
          {% endfor %}

        {% else %}
          <input type="{{ field.type }}" id="field_{{ form.id }}_{{ field.id }}" name="input_{{ field.id }}"
            {% if field.placeholder %}placeholder="{{ field.placeholder }}"{% endif %}
            {% if field.required %}required{% endif %}
            {% if field.maxLength %}maxlength="{{ field.maxLength }}"{% endif %}>
        {% endif %}
      </div>
    {% endif %}
  {% endfor %}
  <button type="submit" class="gform-submit">Submit</button>
</form>
{% endmacro %}
