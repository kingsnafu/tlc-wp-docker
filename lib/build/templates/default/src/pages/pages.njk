---
pagination:
  data: pages
  size: 1
  alias: page
permalink: "/{{ page.slug }}/index.html"
eleventyComputed:
  title: "{{ page.title }}"
  seo_description: "{{ page.seo_description }}"
layout: page.njk
---
{{ page.content | safe }}
