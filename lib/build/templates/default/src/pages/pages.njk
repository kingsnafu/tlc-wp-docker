---
pagination:
  data: pages
  size: 1
  alias: page
permalink: "/{{ page.slug }}/index.html"
eleventyComputed:
  title: "{{ page.title | safe }}"
  seo_description: "{{ page.seo_description | safe }}"
layout: page.njk
---
{{ page.content | safe }}
