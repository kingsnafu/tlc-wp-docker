---
pagination:
  data: posts
  size: 1
  alias: post
permalink: "/blog/{{ post.slug }}/index.html"
eleventyComputed:
  title: "{{ post.title }}"
  date: "{{ post.date }}"
  seo_description: "{{ post.seo_description }}"
layout: post.njk
---
{{ post.content | safe }}
